# ─── Python ───────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.Python
*.so

# ─── Virtual environments ─────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ─── Tooling caches ───────────────────────────────────────────────────────
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.tox/

# ─── IDE / OS ─────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
.DS_Store

# ─── Simülasyon çıktı dizinleri (üretilen, ~6 GB) ─────────────────────────
# OpenFOAM / CalculiX case ve sonuç klasörleri — kaynak değil, artefakt.
aoa_polar/
mesh_independence/
prism_validation/
transition/
test_cfd_run/
test_fea_run/
_fea_val/
_fea_val_hole/
_fea_val_cyl/
_fea_val_grav/
_fea_val_buckling/
rocket_cfd_case/
rocket_fin/
structural_assessment/
dist_load_test/
c2dval/
cgrid_val/
cgrid_work/
fea_cases/
validation/
simulations/
report/
logs/
vspaero_results/
vspaero_run/
gci_*/
tr_[0-9]*/
tr_mesh/
vehicle_runs/
ogrid_test/
cgrid_test/

# ─── Şartname PDF'leri (bilgi tabanına ait, kod reposuna değil) ───────────
sources/*.pdf

# ─── OpenFOAM artefaktları (case dizinleri dışında kalanlar) ──────────────
processor*/
postProcessing/
*.foam

# NOT: Çözücü çıktıları (*.frd, *.dat, *.cvg, *.12d, *.sta) yukarıdaki case
# dizinlerinin içinde; global ignore EKLENMEDİ çünkü airfoil .dat ve örnek
# .msh dosyaları meşru KAYNAK girdiler (ör. Construct2D/sample_airfoils).

# ─── Kök dizine yazılan sonuç JSON'ları (üretilen) ────────────────────────
# NOT: materials.json ve config.yaml KAYNAK — izlenir, ignore edilmez.
envelope.json
fea_critical.json
coupling_result.json
vspaero_polar.json
aoa_polar.json
openrocket_result.json
rocket_cfd_result.json
rocket_fin_result.json
mesh_quality.json

# ─── Üretilen geometri / mesh dosyaları ───────────────────────────────────
scanned_object.stl
test_sphere.stl

# ─── VSPAERO scratch çıktıları (bridge cwd=kök'e döker) ───────────────────
Unnamed.*
*.vspaero
*.vspgeom
*.adb
*.adb.cases

# ─── Loglar ───────────────────────────────────────────────────────────────
*.log
test_output.log

# ─── Üretilen test çıktıları ──────────────────────────────────────────────
test_materials_export.csv

# ─── Vendored third-party araç (kendi git geçmişi var) ────────────────────
# Construct2D: airfoil C-grid mesh üreteci. construct2d_bridge.py disk'ten
# çağırır; kaynak ağacımıza dahil değil.
Construct2D/
auto_pilot_memory.jsonl

# ─── İndirilen gerçek-dünya CAD modelleri (üçüncü-taraf STL, MB boyutlu) ───
# experiments/ingest_real.py URL'lerden indirir; yalnız türetilen metrikler
# (auto_pilot_real_seed.jsonl, 6 sayı/model) commit'lenir, STL'ler değil.
experiments/real_geo/
stress_topopt_lbracket.png
fsi_divergence.png
mesh_gci_campaign.log
vehicle_runs/_batch_geo/
batch_learn.log
batch_learn_done.json
_fea_val_thermal/

# ─── TMR drag GCI: NASA gridleri (124MB, indirilebilir naca0012-grids.zip) + case'ler ───
# Scriptler (tmr_cfd/*.py) commit'lenir; gridler ve çözülmüş case'ler (mesh/processor/
# postProcessing) türetilmiş/büyük veridir — tmr_gci_verdict.json sonucu taşır.
tmr_grids/
tmr_cfd/n0012_*/
# ccx çalışma artefaktları (FEA element-order, bespoke xfine)
_fea_elem_order/
gci_cgridP_smoke.json
gci_cgridP_xfine.json
# standalone demo çıktısı (V&V verdikti değil)
supersonic_slender_result.json

# ─── Kök dizine bırakılan büyük ikili belgeler (kaynak değil) ─────────────
# Word belgeleri (Bel*.docx — teklif/makale taslakları k220\paper'a ait) ve
# arşivler kazara 'git add -A' ile commit'lenmesin. Kaynak ağacımıza dahil değil.
*.rar
*.docx
*.zip

# ─── Araç/oturum yerel dosyaları ──────────────────────────────────────────
.agents/
.claude/launch.json
validation_anchors_runs/
_doe_runs/
_doe_tmp/
design_explore_*.json
